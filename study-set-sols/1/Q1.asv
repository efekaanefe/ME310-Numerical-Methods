clc; clear;

f = @(x) 2.5./sin(pi-2/3*pi-x) + 2./sin(x); % x->rad 
fplot(f) % x = 0.44 rad

x = deg2rad(0:0.01:60);
y = f(x);

prevState = "decreas";
for i=2:size(y,2)
    if y(i-1)<y(i)
        currState = "inc"
        break
    end

end
