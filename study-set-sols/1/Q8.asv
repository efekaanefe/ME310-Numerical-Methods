clc; clear;

for k = 1:15
    x = 10^(-k);
    result = (exp(x) - 1) / x;
    disp([num2str(x), result])
end
disp(results);

disp("  --------------------")

for k = 1:15
    x = 10^(-k);
    result = (exp(x) - 1) / log(exp(x));
    fprintf("%.")
    disp([num2str(x), result])
end
disp(results_alternative);
